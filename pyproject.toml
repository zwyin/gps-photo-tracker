[project]
name = "gps-photo-tracker"
version = "0.9.0"
description = "Batch process photos with GPS tracks (GPX/KML/TCX) to write EXIF GPS tags"
license = {text = "GPL-3.0-or-later"}
requires-python = ">=3.11"
dependencies = [
    "PySide6>=6.6",
    "piexif>=1.1.3",
    "gpxpy>=1.6.0",
    "Pillow>=10.0.0",
    "geopy>=2.4.0",
    "tenacity>=8.0.0",
]

[project.urls]
Repository = "https://github.com/zwyin/gps-photo-tracker"

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "pytest-qt>=4.0",
]

[project.scripts]
gps-photo-tracker = "gps_photo_tracker.__main__:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["slow: marks tests as slow"]

[tool.coverage.run]
source = ["src/gps_photo_tracker"]

[tool.coverage.report]
fail_under = 75

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
